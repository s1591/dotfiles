colorscheme dracula

clear
add ~/Music/songs
update-cache -f
echo loaded music library 🎉

set mouse=false

set progress_bar=line

set show_current_bitrate=true

set stop_after_queue=true

bind -f common u shell cmus-remote -C "clear"; cmus-remote -C "add ~/Music/songs"; cmus-remote -C "update-cache -f"; cmus-remote -C "echo re-scanned music directory 👍"
